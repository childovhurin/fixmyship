import React from 'react';


function Login() {
    return (
        <div>
            Login Screen with username and password inputs
            (Could use email instead of username)
        </div>
    );
    }

    export default Login;